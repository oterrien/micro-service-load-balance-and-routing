server:
  port: ${port:8090}

spring:
  application:
    name: client
  cloud:
    consul:
      host: localhost
      port: 8500

ribbon:
  ServerListRefreshInterval: 1000

endpoints:
  health:
    enabled: true
    sensitive: false
  restart:
    enabled: true
  shutdown:
    enabled: true

management:
  security:
    enabled: false